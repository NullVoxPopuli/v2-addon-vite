import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | example', function (hooks) {
  setupTest(hooks);

  test('a basic test', async function (assert) {
    assert.strictEqual(2, 2);
  });
});
